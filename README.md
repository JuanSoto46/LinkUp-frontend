# LinkUp Frontend (Sprint 1)

Vite + React + TS + Tailwind. Firebase client auth (email, Google, Facebook).
Sitemap, About, Explore (player+chat placeholders), Register/Login/Reset, Profile (update/delete), Create meeting.

## Run local
```bash
cp .env.example .env
npm i
npm run dev
```
